<task>
Implement Sprint 2: Teacher Onboarding & Approval Workflow for the Django tutoring marketplace project at C:/Users/h7304/Desktop/New folder (2).

Current state after Sprints 0 and 1:
- Django project `tutors_marketplace` is set up with 14 apps.
- Custom `accounts.User` model exists with roles: student, teacher, parent, admin.
- Login/register/logout and role-based dashboards are working.
- `subjects.Subject` and `subjects.GradeLevel` models exist and are seeded.
- No teacher profile, certificates, or approval workflow exists yet.

What to build in this sprint:
1. Create `teachers.TeacherProfile` model with fields:
   - One-to-one to `accounts.User` (teacher role)
   - bio, experience_years, headline
   - photo, cv_file, intro_video_url
   - approval_status choices: pending, approved, rejected, suspended
   - average_rating, total_reviews, total_lessons (defaults 0)
   - verification_notes (for admin)
   - created_at, updated_at
   - Add constraints so only users with role='teacher' can have a profile.

2. Create `teachers.TeacherCertificate` model:
   - ForeignKey to TeacherProfile
   - title, issuing_organization, file, uploaded_at

3. Build teacher onboarding views (in `teachers/views.py`):
   - `setup_profile` — multi-step/profile form for teacher to complete bio, experience, headline, photo, CV, intro video URL.
   - `upload_certificate` — add certificates.
   - `my_status` — view current approval status and admin notes.
   - Protect these views so only authenticated teachers can access.

4. Create templates in `templates/teachers/` for the views above, extending `base.html`.

5. Implement admin teacher review flow in `adminpanel/views.py` and templates:
   - `pending_teachers` list
   - `review_teacher` detail page with documents/certificates
   - Actions: approve, reject (with verification_notes), suspend.
   - Only staff/superusers can access.

6. Approval behavior:
   - New TeacherProfile defaults to `pending`.
   - Only `approved` teachers can appear publicly (we will build public discovery in Sprint 4).
   - Rejected teachers see status and notes.
   - Suspended teachers are treated like rejected for public visibility.
   - Add a helper method/property `is_publicly_visible` or `can_receive_bookings`.

7. Teacher dashboard (`templates/teachers/dashboard.html`):
   - Update it to show approval status, profile completeness hints, and links to setup profile, add certificates, view status, and (when approved) manage availability/pricing.
   - Keep pricing/availability as placeholders for future sprints.

8. URL wiring:
   - Add teacher onboarding URLs under `/teachers/` namespace.
   - Add admin review URLs under `/adminpanel/review/`.

What to leave untouched:
- Do NOT modify the custom User model in `accounts/`.
- Do NOT build Subject/Grade/Pricing/Availability assignment yet (Sprint 3).
- Do NOT build public teacher discovery yet (Sprint 4).
- Do NOT build booking/payment flows yet (Sprints 5-6).
- Keep `core`, `students`, `parents` apps untouched except for any template link updates needed for teacher dashboard navigation.

Follow existing project conventions:
- Use Django templates, plain CSS in `static/css/main.css`, vanilla JS.
- Use `django.contrib.auth.decorators.login_required` and staff checks.
- Business logic should live in model methods / managers or form validation where reasonable, not all in views.
- Use ModelForms for teacher profile and certificate.
- Add `__str__`, `Meta` ordering, and type hints where helpful.
</task>

<verification_loop>
Run these commands in the project root and fix any failures before finishing:
  "C:/Users/h7304/Desktop/New folder (2)/venv/Scripts/python" manage.py check
  "C:/Users/h7304/Desktop/New folder (2)/venv/Scripts/python" manage.py makemigrations --check
  "C:/Users/h7304/Desktop/New folder (2)/venv/Scripts/python" manage.py migrate
  "C:/Users/h7304/Desktop/New folder (2)/venv/Scripts/python" manage.py runserver 8000
Confirm the development server starts without errors and stays up for at least 5 seconds.

Also verify with a quick Django shell command that:
  - A teacher user can have a TeacherProfile created.
  - The profile defaults to pending.
  - Admin approve/reject/suspend status transitions work.
</verification_loop>

<action_safety>
Keep changes scoped to Sprint 2. No unrelated refactors, renames, or cleanup. Do NOT run `git add` or `git commit`. Leave all changes uncommitted in the working tree. Do not create a new virtual environment or modify `requirements.txt` unless absolutely necessary for this sprint.
</action_safety>

<structured_output_contract>
End your run with a report in this exact shape:
  1. What changed and why
  2. Files touched
  3. Gate outcomes (copy the output of check, makemigrations, migrate, and whether runserver started)
  4. Anything you deviated from the brief, left open, or want a decision on
</structured_output_contract>
